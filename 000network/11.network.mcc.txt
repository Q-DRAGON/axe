1，总时间 4 小时

2，进度： 《TCP/IP详解卷2——实现》 第五章 至 第七章

3，学习的内容有:

**CH5 接口：SLIP和还回
1）SLIP接口的结构和函数方法。
2）SLIP的性能考虑。
3）环回接口如何按目的地址分用输出分组，并如何将分组放入响应的输入队列中。

**CH6 IP编址
1）IP模型中地址指派给网络接口而不是系统本身。
2）IP编址机制。
3）IP专用的接口地质结构和协议地址结构。
4）如何用ifconfig和ioctl接口命令来配置接口的IP专用信息。

**CH7 域和协议
1）Net/3数据结构：可以同时操作多个网络协议。
2）协议域由domain结构表示。
3）每个协议中被分配一个protosw结构，这些结构被组织在一个数组中。
4）inetdomain和数组inetsw描述Internet协议。
5）系统初始化时，domainint将链接到domains列表中，调用域和协议初始化函数，并调用快速和慢速超时函数。

4，掌握程度：
了解了相关数据的结构和操作函数。
